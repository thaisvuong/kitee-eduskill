# Kientre Workspace

Hermes profile: `cmkitee`.

## Primary workspace
- Local folder: `/Users/nguyenthaivuong/Desktop/HermesWorkSpace/Kientre`
- Google Drive name: `Kientre`
- Google Drive folder: https://drive.google.com/drive/folders/18fe276zrUdVAlFOFyKHPtc_8-GpEexKn?usp=sharing

## Operating rules for this workspace
- Treat this folder as the main local working directory for Kientre-related work.
- When the user mentions “Kientre”, use this local folder and the Kientre Google Drive link as the default project context.
- Keep files, drafts, notes, and workspace artifacts for this profile under this folder unless the user specifies another location.
- For Zalo/bot communication for this profile, focus on this Kientre workspace and reference the Kientre Drive link when cloud/shared storage is needed.

## Kientre access
- This `cmkitee` profile is allowed to use the full Kientre workflow/skills for education-document tasks.
- Kientre working copy: `/Users/nguyenthaivuong/Desktop/HermesWorkspace/Kientre`.
- When the user asks for lesson plans, worksheets, exams, tutoring schedules, or Vietnamese primary-school math documents, load/use the relevant `education/*` skills, especially `edu-skill`.
- Store or upload education outputs under the Kientre workspace/Drive context unless the user specifies another destination.

## Google Workspace access
- This profile has profile-local Google OAuth files at `/Users/nguyenthaivuong/.hermes/profiles/cmkitee/google_token.json` and `google_client_secret.json` copied from the main Hermes setup; never print their contents.
- Use the profile-local Google Workspace skill/script with `HERMES_HOME=/Users/nguyenthaivuong/.hermes/profiles/cmkitee` for Drive/Docs/Sheets/Calendar operations.
- For requests like `/review https://docs.google.com/document/d/<DOC_ID>/edit` or Zalo text such as `Link này em nhé: <Google Docs/Drive URL>`, extract the Google Doc/Drive file ID from the URL first. Before reading file contents, verify the file is inside the Kientre Drive folder (`18fe276zrUdVAlFOFyKHPtc_8-GpEexKn`) or one of its subfolders. If it is inside Kientre, read it with `google-workspace/scripts/google_api.py docs get <DOC_ID>` (for Docs) or the appropriate Drive command, then review/summarize in Vietnamese unless the user asks otherwise. When the returned document contains `images[]`, analyze the images as part of the review using vision/OCR: describe diagrams/screenshots, read visible text, check math/worksheet content shown in images, and mention image-based findings separately from text findings. If it is outside Kientre, do not read text or image contents; explain that the file is outside the allowed Kientre scope and ask the user to move/copy it into Kientre or explicitly authorize this specific external link.
- For Zalo bot messages that mention “link”, “Drive”, “Docs”, “tài liệu”, “file vừa gửi”, or “link vừa gửi” but contain no visible URL, assume Zalo may have hidden/filtered the link preview. Do not say you cannot help immediately: first use Google Workspace to search recent/relevant Drive or Docs files strictly inside the Kientre Drive folder (`18fe276zrUdVAlFOFyKHPtc_8-GpEexKn`) and its subfolders only, infer the likely document, and ask for confirmation if multiple matches exist. Do not search or access files outside the Kientre folder unless the user explicitly authorizes a broader scope in that conversation.
- For generated education/workspace files, save locally under the Kientre workspace and upload to the Kientre Drive folder when the user asks to share/store in cloud.
- Current OAuth is verified for Drive/Docs. Gmail read/send may require re-authorization with Gmail scopes before email workflows.

## Private Zalo bot / gateway route
- This workspace is the default scope for the private Zalo bot attached to profile `cmkitee`.
- Hermes default gateway is running in multi-profile/multiplexer mode and exposes this profile via the `/p/cmkitee/` URL prefix.
- Webhook listener runs locally on `127.0.0.1:8754`.
- Zalo bridge route: `zalo-kientre` at `http://127.0.0.1:8754/p/cmkitee/webhooks/zalo-kientre`.
- Hermes currently needs a Zalo bridge/public callback layer for real Zalo OA integration; keep bridge notes in `ZALO_BOT_SETUP.md`.
