# Kitee Workspace

Hermes profile: `cmkitee`.

## Primary workspace
- Local folder: `/Users/nguyenthaivuong/Desktop/HermesWorkSpace/Kitee`
- Google Drive name: `Kitee`
- Google Drive folder: https://drive.google.com/drive/folders/18fe276zrUdVAlFOFyKHPtc_8-GpEexKn?usp=sharing

## Operating rules for this workspace
- Treat this folder as the main local working directory for Kitee-related work.
- When the user mentions “Kitee”, use this local folder and the Kitee Google Drive link as the default project context.
- Keep files, drafts, notes, and workspace artifacts for this profile under this folder unless the user specifies another location.
- For Zalo/bot communication for this profile, focus on this Kitee workspace and reference the Kitee Drive link when cloud/shared storage is needed.

## eduSkill access
- This `cmkitee` profile is allowed to use the full eduSkill workflow/skills for education-document tasks.
- eduSkill working copy: `/Users/nguyenthaivuong/Desktop/HermesWorkspace/eduSkill`.
- When the user asks for lesson plans, worksheets, exams, tutoring schedules, or Vietnamese primary-school math documents, load/use the relevant `education/*` skills, especially `edu-skill`.
- Store or upload education outputs under the Kitee workspace/Drive context unless the user specifies another destination.

## Google Workspace access
- This profile has profile-local Google OAuth files at `/Users/nguyenthaivuong/.hermes/profiles/cmkitee/google_token.json` and `google_client_secret.json` copied from the main Hermes setup; never print their contents.
- Use the profile-local Google Workspace skill/script with `HERMES_HOME=/Users/nguyenthaivuong/.hermes/profiles/cmkitee` for Drive/Docs/Sheets/Calendar operations.
- For requests like `/review https://docs.google.com/document/d/<DOC_ID>/edit`, extract the Google Doc ID, read it with `google-workspace/scripts/google_api.py docs get <DOC_ID>`, then review/summarize the document in Vietnamese unless the user asks otherwise.
- For Zalo bot messages that mention “link”, “Drive”, “Docs”, “tài liệu”, “file vừa gửi”, or “link vừa gửi” but contain no visible URL, assume Zalo may have hidden/filtered the link preview. Do not say you cannot help immediately: first use Google Workspace to search recent/relevant Drive or Docs files strictly inside the Kitee Drive folder (`18fe276zrUdVAlFOFyKHPtc_8-GpEexKn`) and its subfolders only, infer the likely document, and ask for confirmation if multiple matches exist. Do not search or access files outside the Kitee folder unless the user explicitly authorizes a broader scope in that conversation.
- For generated education/workspace files, save locally under the Kitee workspace and upload to the Kitee Drive folder when the user asks to share/store in cloud.
- Current OAuth is verified for Drive/Docs. Gmail read/send may require re-authorization with Gmail scopes before email workflows.

## Private Zalo bot / gateway route
- This workspace is the default scope for the private Zalo bot attached to profile `cmkitee`.
- Hermes default gateway is running in multi-profile/multiplexer mode and exposes this profile via the `/p/cmkitee/` URL prefix.
- Webhook listener runs locally on `127.0.0.1:8754`.
- Zalo bridge route: `zalo-kitee` at `http://127.0.0.1:8754/p/cmkitee/webhooks/zalo-kitee`.
- Hermes currently needs a Zalo bridge/public callback layer for real Zalo OA integration; keep bridge notes in `ZALO_BOT_SETUP.md`.
